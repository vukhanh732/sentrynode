# SentryNode

Portfolio SIEM project (FastAPI + RabbitMQ + Elasticsearch + InfluxDB + Grafana).

## Local dev (Phase 1)
1. cp .env.example .env
2. docker compose up -d --build
3. curl http://localhost:8000/health
